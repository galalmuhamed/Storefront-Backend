import dotenv from 'dotenv';

dotenv.config();
const {
	PORT,
	POSTGRES_HOST,
	POSTGRES_PORT,
	POSTGRES_DB,
	POSTGRES_DB_TEST,
	POSTGRES_USER,
	POSTGRES_PASSWORD,
	ENV,
	BCRYPT_PASSWORD,
	SALT_ROUNDS,
	TOKEN_SECRET,
} = process.env;

export default {
	PORT,
	POSTGRES_HOST,
	POSTGRES_PORT,
	POSTGRES_DB,
	POSTGRES_DB_TEST,
	POSTGRES_USER,
	POSTGRES_PASSWORD,
	ENV,
	BCRYPT_PASSWORD,
	SALT_ROUNDS,
	TOKEN_SECRET,
};
